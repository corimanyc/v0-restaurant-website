/* ---------------------------------------------------------------------------
 * SITE CONFIG — PLACEHOLDERS
 * Replace these values with the real restaurant details. They drive the
 * metadata, Open Graph/Twitter cards, sitemap, robots, manifest, and the
 * Restaurant structured data (JSON-LD).
 * ------------------------------------------------------------------------- */

// Production domain (no trailing slash).
export const SITE_URL = 'https://www.corimanyc.com'

export const SITE_NAME = 'CORIMA'

// Shown in social link previews (iMessage, Slack, X, etc.) under the title.
// Kept intentionally minimal — just the brand name.
export const SITE_DESCRIPTION = 'CORIMA'

// Used for the Google search snippet and the meta description / JSON-LD. This
// is decoupled from SITE_DESCRIPTION so search results read as a proper,
// descriptive sentence while social cards stay clean.
export const SITE_TAGLINE =
  'CORIMA is a contemporary Mexican restaurant in New York from Chef Fidel Caballero, serving a seasonal tasting menu of progressive Northern Mexican cuisine alongside à la carte dishes, signature cocktails, and a curated wine list.'

// 1200x630 social-share image generated from the first landing carousel photo.
export const OG_IMAGE = '/og-corima.jpg'
